{
  "name": "@openforge/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build --prefix ../shared && tsx watch src/server.ts",
    "build": "npm run build --prefix ../shared && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "npm run typecheck --prefix ../shared && tsc --noEmit",
    "test": "echo \"No backend tests configured yet\""
  },
  "dependencies": {
    "@openforge/shared": "file:../shared",
    "@supabase/supabase-js": "^2.108.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
